# Published packages to install into the Alliance Auth image, one per line,
# pinned so a rebuild is reproducible. Rebuild after editing:
#   docker compose up -d --build
#
# Bump the pin below to test a newer plugin release; the plugin source itself
# lives in its own repo (https://github.com/ryanrolds/aa-wanderer-leaderboard).

aa-wanderer-leaderboard==0.1.0
